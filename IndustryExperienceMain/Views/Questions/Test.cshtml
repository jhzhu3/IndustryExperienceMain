@model Tuple<List<Question>, List<Answer>>
@{
    ViewData["Title"] = "Test";
}

<h1>Test</h1>

@*@{
    var QuestionList = Model.ToList();
    foreach (var item in QuestionList)
    {
        @Html.DisplayFor(modelItem => item.Question);
    }

}*@
@*@using (Html.BeginForm("Test", "Questions", FormMethod.Post))
{
    var QuestionList = Model.Item1.ToList();
    var AnswerList = Model.Item2.ToList();
    foreach (var question in QuestionList)
    {
        <div>
            <h2>@question.Text</h2>
            <ul>


                @foreach (var answer in AnswerList)
                {
                    @if (answer.QuestionId.Equals(question.Id))
                    {
                        <li>@Html.RadioButton("SelectedAnswerId", Model.Item2.Select(a => a.Id.Equals(answer.Id))) @answer.Text</li>
                    }
                }

            </ul>
        </div>
    }

    <input type="submit" value="submit"></input>

    <br></br>

    <h4>You Selected:</h4>
    <b>Answer:</b>@ViewBag.SelectedAnswerId
}*@

@using (Html.BeginForm("TestTwo", "Questions", FormMethod.Post))
{
    var QuestionList = Model.Item1.ToList();
    var AnswerList = Model.Item2.ToList();
    var question1 = QuestionList[0];
    <h2>@question1.Text</h2>
    <ul>
        @foreach (var answer in AnswerList)
        {
            if (answer.QuestionId.Equals(question1.Id))
            {
                <li>@Html.RadioButton("answer1", answer.Points) @answer.Text</li>
            }
        }
    </ul>
    <input type="submit" value="submit"></input>
}